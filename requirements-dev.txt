# NOTE:
# - requirements-dev.txt is the editable, range-based dev input.
# - For deterministic builds, generate and use requirements-dev.lock (with hashes):
#     make lock
#
# Dev/test dependencies (pip-tools input)
-r requirements.in

pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
pip-tools>=7.4.1,<8.0
