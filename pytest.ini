[pytest]
testpaths = tests
# Browser smoke is opt-in — keeps `pytest` fast and deterministic without live URLs / Chromium.
addopts = -q -m "not playwright"
markers =
    playwright: browser-based smoke tests requiring Playwright and a running app stack
